# KrokantBackend

ASP.NET Core Web API for educational task management (SQLite, EF Core, JWT).

# Requirements

.NET 9

# Start

```bash
cd KrokantBackend
dotnet run
```

Default base URL: `https://localhost:5001` or `http://localhost:5000` and API base `/api/v1`.
